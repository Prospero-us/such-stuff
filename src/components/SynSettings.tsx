import React, { useState, useEffect } from 'react';

interface SynSettingsProps {
  isOpen: boolean;
  onClose: () => void;
}

export const SynSettings: React.FC<SynSettingsProps> = ({ isOpen, onClose }) => {
  // The component is now simplified and not used in the current design
  // We keep it minimal for future reintroduction if needed
  
  return null;
}; 